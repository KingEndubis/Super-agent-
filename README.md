# Super Agent — Hybrid (Puter.js + Python)

This repo contains a hybrid Super Agent skeleton:
- Node/ Putter.js entry + HTTP bridge
- Python core agents (finance, privacy, validator)
- Minimal UI with speech-to-text / text-to-speech (browser)

## Quick start (local)
1. Install Node deps:
   ```bash
   npm install
